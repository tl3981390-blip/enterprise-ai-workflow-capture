# v2.0.1 acceptance evidence

Date: 2026-09-01. Environment: Windows, Python 3.14.6, cryptography 50.0.1, PyYAML 6.0.3, SQLite 3.50.4. Root cause closed: harness-trusted inputs were not actually attested in v2.0.0.

## Final 16-question verdict (contract §25)

| # | Question | Verdict |
|---|---|---|
| 1 | Host Model 能否自己创建合法 Enterprise Authorization？ | 不能 — PASS（unsigned/自签/伪造全部 fail closed；TRUST-001/002/003） |
| 2 | Host Model 能否使用 Runtime environment 的 signer 给自己授权？ | 不能 — PASS（生产模式无可用 signer；本地 signer 输出被标记且在生产模式被拒绝；TRUST-004） |
| 3 | Host Model 能否自己选择 verification public key / trust root？ | 不能 — PASS（payload 字段机械拒绝；无 CLI 入口；模型 key 不在受信根即验证失败；TRUST-003） |
| 4 | Host Model 能否伪造 capture_session_id？ | 不能 — PASS（enterprise candidate 携带即拒绝；唯一来源是 verified assertion；TRUST-005/006） |
| 5 | Host Model 能否通过字符串声明 harness_provided？ | 不能 — PASS（enterprise candidate 的 harness_provided 声明被拒绝；仅运行时注入；TRUST-007） |
| 6 | Department/workflow 是否真正来自 verified Harness Context？ | 是 — PASS（注入自 verified assertion；mismatch/伪造拒绝；TRUST-007/008/012） |
| 7 | Assertion 是否绑定 scope/session/storage/context？ | 是 — PASS（scope/storage/session/context/issuer/有效期/identity 全部签名绑定并逐项校验；TRUST-009/011/012） |
| 8 | Assertion 篡改是否 Fail Closed？ | 是 — PASS（5 个签名字段逐一篡改均拒绝；TRUST-011） |
| 9 | Unsigned Enterprise Assertion 是否 Fail Closed？ | 是 — PASS（生产路径不再有 harness_asserted_unverified 成功态；TRUST-001；本地 grant 仅 DEVELOPMENT_TEST_ONLY） |
| 10 | Development test signer 是否无法产生 Production Authority？ | 是 — PASS（trust_class 机械标记；PRODUCTION_ENTERPRISE 模式拒绝 dev 信任根与 grant；make_test_assertion 拒绝生产 class；TRUST-004/013、mode 测试） |
| 11 | 原 76 tests 是否全部无回归？ | 是 — PASS（76 全保留通过；两处断言按新信任语义加强而非放宽） |
| 12 | 新 Trust Boundary attacks 是否全部 PASS？ | 是 — PASS（18 个新测试，TRUST-001~013 + 模式/构造守卫） |
| 13 | Clean install 是否 PASS？ | 是 — PASS（ZIP 解包→install.py→doctor→资产自身 94 测试 OK） |
| 14 | Harness discovery 是否 PASS？ | 是 — PASS（check_release_hygiene.py：SKILL.md 严格 YAML、name/version 正确、安装目录唯一可发现） |
| 15 | GitHub redownload 是否 PASS？ | 是 — PASS（下载资产 SHA 与 SHA256SUMS 一致；解包后全量测试+Trust 攻击+签名 assertion E2E 通过） |
| 16 | Final Product Target 是否 PASS？ | 是 — PASS（含"模型不能伪造企业采集授权和 Harness 上下文"） |

`FINAL_PRODUCT_TARGET = PASS`　`v2.0.1 STABLE = PASS`

残余项（PENDING_EXTERNAL_VALIDATION）：Harness 受保护环境/信任根与私钥托管、组织专属密级分类、生产多用户授权与加密/备份/留存、企业适配器部署认证、外部业务系统身份映射。

## Verified on the final code

- **Full regression**: `python -m unittest discover -s tests` → 94/94 PASS（76 preserved + 18 new）。
- **Compile**: `python -m compileall -q workflow_capture scripts tests` → PASS。
- **Release hygiene**: `python scripts/check_release_hygiene.py --version 2.0.1` → PASS。
- **Clean room**（`.tmp/v201-cleanroom`，12/12 PASS）: offline install from the release asset; installed-copy doctor; strict YAML + unique discovery; test-minter assertion E2E (`asymmetric_signature_verified`, session/context injected, `DEVELOPMENT_TEST_ONLY` honestly marked); tampered assertion refused (exit 4, no db created); forged session id refused (exit 2); dev grant refused under `PRODUCTION_ENTERPRISE`; dev grant path persisted with dev trust class; fail-closed without authorization; personal TTY guard; asset's own 94-test suite.
- **Credential scan**: redaction-engine scan of the repository; only synthetic runtime-assembled fixtures and detector-regex source — no credential present. Runtime asset contains no `Ed25519PrivateKey` usage (TRUST-013).

## Release identity

- Tag `v2.0.1` → exact final commit (see git log / GitHub Release).
- Asset `enterprise-ai-workflow-capture-v2.0.1.zip`; SHA-256 in `dist/SHA256SUMS.txt` and the GitHub Release; redownload-verified.
- v2.0.0 tag/release/asset verified untouched.
