# v1.0.0 acceptance evidence

Candidate identity is established by the final Git commit, immutable `v1.0.0` tag, GitHub Release and asset SHA-256. Before tagging, rerun all commands against the final clean worktree.

Verified locally on 2026-08-31:

- `python -m unittest discover -s tests -v` → 16 tests, PASS.
- `python -m compileall -q workflow_capture scripts tests` → PASS.
- `python scripts/flow_capture.py prepare/commit/show/similar/doctor` → PASS; confirmed payload hash matched read-back; database integrity `ok`.
- `quick_validate.py .` with `PYTHONUTF8=1` → `Skill is valid!`.
- `python scripts/build_release.py --version 1.0.0` → deterministic versioned ZIP created.
- Extracted Release ZIP → `scripts/install.py` → installed-copy `doctor` → PASS.

See `docs/RELEASE_NOTES_v1.0.0.md` for external validation boundaries.

