# v1.0.1 acceptance evidence

The final Release identity is established by the immutable remote `v1.0.1` tag, Release commit and downloadable asset digest.

Verified locally before release:

- `python -m unittest discover -s tests -v` → 20 tests PASS.
- Unconfirmed CLI commit → exit 2; no Task persisted.
- PTY confirmation E2E → `PREPARED → CONFIRMED → CONSUMED`; second consumption → exit 2.
- Forged internal `content_hash` → rejected before database creation.
- Stored Evidence tamper → Doctor exit 1 with failed event ID while SQLite integrity remained `ok`.
- Formal GitHub v1.0.0 asset created a schema-v2 database; v1.0.1 migrated it to v3, read back the old Task, classified its confirmation as `CONSUMED/legacy_v2`, and classified old Evidence as `legacy_unverified`.
- Skill structural validation and installed-copy Doctor must pass again on the final Release candidate.

The PTY E2E used an explicitly labeled synthetic test identity. It is evidence of the mechanical terminal boundary, not a claim that a real employee confirmed a production record.
