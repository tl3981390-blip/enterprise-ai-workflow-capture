# v2.0.0 acceptance evidence

Date: 2026-09-01. Environment: Windows, Python 3.14.6, SQLite 3.50.4. Baseline reconciled in `V2_TARGET_GAP_REPORT.md`; understanding contract and plan gates passed (`evidence/v2-task-understanding-contract.json`, `evidence/v2-delivery-plan.json`, both mechanical gates PASS).

## Final product target (contract §19)

> 企业在 AI 尚未深入接入业务流程时，是否可以规定员工调用这个 Skill，并以非常低的额外负担，安全、真实、结构化地持续积累"任务到底是怎么完成的"数据，为以后发现更好的流程和 AI 自动化机会提供可靠基础？

**YES — PASS**, supported by the evidence below. Residuals are honestly marked PENDING_EXTERNAL_VALIDATION / NOT_INCLUDED_BY_DESIGN in `docs/RELEASE_NOTES_v2.0.0.md` and `evidence/v2.0.0-adversarial.md`.

## Verified on the final code (commit series ending at the v2.0.0 tag)

- **Full regression**: `python -m unittest discover -s tests` → 76/76 PASS (20 preserved v1 personal-mode regression + 56 v2). Re-run after the last code change: PASS.
- **Compile**: `python -m compileall -q workflow_capture scripts tests` → PASS.
- **Structure**: SKILL.md frontmatter (name/description/license/version 2.0.0) and all README/SKILL internal references resolve → PASS. (The upstream `quick_validate.py` referenced by v1 docs does not exist in the installed skill-creator 0.1.0 plugin; the delivery skill's own validator is specific to its module tree and is NOT_APPLICABLE here — replaced by the mechanical check above.)
- **Credential scan**: entire repository scanned with the shipping redaction engine; findings limited to synthetic test fixtures and two prose false positives — no credential is present.
- **Adversarial matrix**: all 18 scenarios executed; see `evidence/v2.0.0-adversarial.md` — 18/18 PASS, with named residuals.
- **Enterprise E2E (repo)**: grant minted via env-held key (`scripts/make_grant.py`) → `capture` → `TASK_COMPLETED_CAPTURE_PERSISTED`, `hmac_sha256_verified`, `read_back_ok: true`; `show` returns 8 ordered steps with intervention/capability/timing, capture mode, business context (hashed ref) and AI context; `similar` matches on the disclosed normalized-label basis; `doctor` schema 4, chain ok, integrity ok.
- **Clean install**: release ZIP extracted to a clean directory → its own 76/76 suite PASS → `install.py --target` → installed-copy `doctor` ok.
- **Fresh harness simulation** (clean dir, env-only authorization, installed copy): 8/8 checks PASS — install, doctor, env-key grant minting, enterprise capture persisted with HMAC verification, idempotent replay (same `task_id`), fail-closed exit 4 without authorization, personal-mode non-interactive confirmation refused, installed-copy read-back hash match.
- **Storage failure honesty**: simulated timeout → exit 5 + `TASK_COMPLETED_CAPTURE_FAILED` + no partial write; same-session retry persists exactly one record (service-level and CLI-level).

## Release identity

- Tag: `v2.0.0` (see git log; exact commit recorded in the GitHub Release).
- Asset: `enterprise-ai-workflow-capture-v2.0.0.zip`; SHA-256 recorded in `dist/SHA256SUMS.txt` and the GitHub Release.
- v1.0.0/v1.0.1 tags, releases and assets verified untouched.

The PTY/interactive boundary and personal-mode state machine are unchanged from v1.0.1 and remain covered by automated tests; harness-native signed human attestation remains PENDING_EXTERNAL_VALIDATION.
